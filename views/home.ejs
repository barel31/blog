<%- include('partials/header'); %>
    <h1>Home</h1>
    <p><%= startingContent %> </p>
    <% posts.forEach((v, i) => { %>
        <h2><%=v.title%></h2>
        <p><%=v.body.substring(0, 160) + ' ...'%> <a href="/posts/<%=v._id%>">Read More</a></p>
    <% }); %>
<%- include('partials/footer'); %>
